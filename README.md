# version-test

A simple Node.js Express app with multiple versioned branches:
- v1.0.0
- v1.1.0
- v2.0.0

Each branch runs an Express server and responds with its own version string.

Used to test version-tagged scans with Jenkins and Checkmarx.
